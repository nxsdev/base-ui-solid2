@AGENTS.md

Additional note for this repository:

- Treat `packages/solid2/` and `docs/src/app/(docs)/solid2/` as the active Solid 2 beta work areas.
- Use `../base-ui-solid/` as the legacy Solid reference for structure and demo layout, while keeping the current React implementation as the primary source of truth.
